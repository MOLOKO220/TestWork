import "./Footer.scss";

import CompLink from "../CompLink/CompLink";

function Main(props) {
  return (
    <footer>
      <CompLink content={"who?"} url={"testpage"} />{" "}
    </footer>
  );
}

export default Main;
